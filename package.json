{
  "name": "the-waiting-room",
  "private": true,
  "version": "0.1.0",
  "description": "Shelter listings read twice: once flat, once by a voice generated from the record itself.",
  "type": "module",
  "scripts": {
    "start": "node tools/serve.js 8124",
    "test": "node --test tests/*.test.mjs",
    "audio:placeholders": "python tools/make_placeholder_audio.py"
  }
}
